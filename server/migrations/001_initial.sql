-- Historical v1 bootstrap intentionally retired. Existing installations keep
-- their old tables untouched; new installations create only strict v2 tables
-- in 003_zero_content_feedback.sql.
SELECT 1;
